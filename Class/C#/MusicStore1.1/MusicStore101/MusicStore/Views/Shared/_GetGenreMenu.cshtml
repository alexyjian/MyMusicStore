@model IEnumerable<MusicStoreEntity.Genre>

@foreach (var item in Model)
{
    <a href="@Url.Action("","",new { id=item.ID},null)" class="list-group-item">
        <span class="glyphicon glyphicon-music"></span>
        @item.Name
    </a>
}